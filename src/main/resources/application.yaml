spring:
  application:
    name: rust-shop

  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/rust_shop
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update



server:
  port: 8080

